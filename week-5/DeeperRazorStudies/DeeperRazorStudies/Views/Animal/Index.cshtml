@model DeeperRazorStudies.ViewModels.IndexViewModel

@{
    ViewBag.Title = "Index";
}

<h2>You have @Model.Count Animals</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.Animals.Name)
           
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FurColor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Species)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model.Animals) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FurColor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Species)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
